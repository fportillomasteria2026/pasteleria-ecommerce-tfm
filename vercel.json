{
  "installCommand": "cd frontend && npm install",
  "buildCommand": "cd frontend && npm run build && cp -r dist/frontend/browser/* dist/frontend/",
  "outputDirectory": "dist/frontend",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
